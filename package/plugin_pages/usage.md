# Usage

## Runtime component

Add `ExampleRotator` to a game object and adjust **Speed** in the Inspector.

## Editor panel

Open **Extensions → Example Plugin → Example Plugin**.

## Distribution

Build the repository into one `.inxpkg`, or install it directly from its Git URL.
